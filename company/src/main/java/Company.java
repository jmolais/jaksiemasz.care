public class Company {
    void randomCompany(int depth, int maxWidth) { //TODO, will return a composite model of a company - probably an Employee/TeamLeader object
        // that's the CEO of the company and contains all the other leafs/composites.

    }
}
